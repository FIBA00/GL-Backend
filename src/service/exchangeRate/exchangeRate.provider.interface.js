// src/services/exchangeRate/exchangeRate.provider.interface.js
// Documents the contract every provider must implement — not enforced by
// JS at runtime, but keeps future you (or future-project you) honest.
// getRate(commodityCode) => Promise<number>  — ETB value per unit
